version: '2.2'
services:
  master:
    build:
      context: ./master
      dockerfile: master.dockerfile
#    depends_on:
#      - node01
    volumes:
      - ./common:/tmp/common

  # node01:
  #   build:
  #     context: ./nodes
  #     dockerfile: nodes.dockerfile
  #   volumes:
  #     - ./common:/tmp/common